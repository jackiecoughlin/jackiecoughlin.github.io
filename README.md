# Personal Website

Static personal website for Jackie Coughlin.

Open `index.html` in a browser to view it locally. The main content lives in `index.html`,
styling in `styles.css`, and the hero image in `assets/hero-workspace.png`.
